import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Aurulenox',
};

export default function HomePage() {
  return <HomeView />;
}
